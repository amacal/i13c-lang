Registered all 8 SDM Group 1 arithmetic mnemonics (ADD/OR/ADC/SBB/AND/SUB/XOR/CMP) end to end via a shared INSTRUCTIONS_TABLE variant list, a shared blocklet emitter, and a shared base-opcode-driven encoder, implemented explicit byte/word/dword/qword memory-operand size syntax from the AST through to the encoder, and added a general semantic check rejecting instructions that combine a legacy high-byte register with any REX-forcing register, closing with 695 encoding tests and 487 syntax/semantic/core tests passing.
