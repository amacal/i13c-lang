title: "Scaled Indexing and RIP-Relative Data References"
status: not-completed
mentioned_by:
  - "Consuming Register Allocation in the Encoding Backend"
  - "Group 2 Shift and Rotate Instructions, Memory-Operand Widths, and Full Family Registration"
  - "LEA and BSWAP Instruction Test Completeness"
evidence:
  - ".history/2026-08/2026-08-30-push-instruction-test-completeness-and-address-displacement-redesign.yml session.completed (scale addressing syntax, e.g. [rsp + 4 * r15], was implemented end to end this session — grammar, entities/indices.py, resolutions/indices.py, and encoding; this half of the target is resolved, leaving only the RIP-relative-data-reference half below)"
  - ".history/2026-08/2026-08-27-consuming-register-allocation-in-the-encoding-backend.yml session.not_completed (the Fixed literal-value RIP-relative addressing variant exists in the type system and encoder but was never exercised by a real instruction this session; the Relocation-based deferred-value sibling for lea rax, [rel @label], referencing a future .quad-style inline data block via the same block/relocation mechanism as JMP/CALL, was designed in dialogue but not implemented; the .quad-style inline-data-in-.text pattern itself, guarded by unconditional control flow, was also designed conceptually but not implemented)"
  - ".history/2026-09/2026-09-05-group-2-shift-and-rotate-instructions-memory-operand-widths-and-full-family-registration.yml session.explored/not_completed (SHL's pre-existing Fixed/RIP-relative destination support was deliberately dropped rather than carried into the newly-unified Group2Operands type shared by all 8 shift/rotate mnemonics, narrowing this target's scope back down to exactly the RIP-relative-data-reference half; the two now-unsupported rip-row tests in src/tests/encoding/bits.py were removed)"
  - ".history/2026-09/2026-09-05-lea-and-bswap-instruction-test-completeness.yml session.not_completed (LEA's own literal RIP-relative addressing rows, previously covered in the old src/tests/encoding/addr.py via the Fixed variant with base=rip, were not carried into the new exhaust()-based src/tests/encoding/lea.py — expand()'s addr64 case generates no rip-relative form — so this coverage was silently dropped a second time, the same pattern already recorded for SHL above)"
