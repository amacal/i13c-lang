title: "Scaled Indexing and RIP-Relative Data References"
status: not-completed
mentioned_by:
  - "Consuming Register Allocation in the Encoding Backend"
evidence:
  - ".history/2026-08/2026-08-27-consuming-register-allocation-in-the-encoding-backend.yml session.not_completed (*scale addressing syntax, e.g. [rsp + rcx*4], does not exist in the language at all; every index use added this session is implicit scale=1; deliberately deferred)"
  - ".history/2026-08/2026-08-27-consuming-register-allocation-in-the-encoding-backend.yml session.not_completed (the Fixed literal-value RIP-relative addressing variant exists in the type system and encoder but was never exercised by a real instruction this session; the Relocation-based deferred-value sibling for lea rax, [rel @label], referencing a future .quad-style inline data block via the same block/relocation mechanism as JMP/CALL, was designed in dialogue but not implemented; the .quad-style inline-data-in-.text pattern itself, guarded by unconditional control flow, was also designed conceptually but not implemented)"
