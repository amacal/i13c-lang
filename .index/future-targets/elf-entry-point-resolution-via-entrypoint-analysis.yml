title: "ELF Entry Point Resolution via Entrypoint Analysis"
status: not-completed
mentioned_by:
  - "Consuming Register Allocation in the Encoding Backend"
evidence:
  - ".history/2026-08/2026-08-27-consuming-register-allocation-in-the-encoding-backend.yml session.bugs_found (elf_command computed entry_point as ELF_BASE_ADDR + file_off_code, the start of the code section, with no awareness of which blocklet is the program's actual entrypoint; confirmed via strace that the process ran only exit(0) because whichever blocklet was laid out first happened to run; the fix applied reordered blocklet iteration so the real entrypoint ends up first, which works for this single-entrypoint program but does not consult the existing analyses/entrypoints Entrypoint analysis and is not a general fix)"
  - ".history/2026-08/2026-08-27-consuming-register-allocation-in-the-encoding-backend.yml session.not_completed"
