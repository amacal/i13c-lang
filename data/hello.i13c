asm shl(left@imm: u8[0x01..0x0f]) clobbers rdi {
    shl rdi, left;
}

asm exit(code@rdi: u32[0x01..0x0f]) noreturn clobbers rax, rcx, r11 {
    mov rax, 0x3c;
    syscall;
}

fn main() noreturn {
    shl(0x02);
    shl(0x03);
    exit(0x01);
}
