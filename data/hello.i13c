asm exit(code@rdi: u32) noreturn clobbers rax, rcx, r11 {
    mov rax, 0x30;
    syscall;
}

asm exit2() noreturn clobbers rax, rcx, r11 {
    mov rax, 0x3c;
    syscall;
}

fn main() noreturn {
    exit(0x01);
    exit2();
}

fn test() noreturn {
    exit2();
}
